name: sim
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get: ^4.6.6

  # UI
  flutter_screenutil: ^5.9.0
  google_fonts: ^6.2.0

  change_app_package_name: ^1.1.0

  # Signature
  fast_rsa: ^3.8.0

  # Logger
  sentry_flutter: ^9.3.0

  # Icon
  flutter_launcher_icons: ^0.14.3
  permission_handler: ^12.0.0+1
  dio: ^5.8.0+1
  flutter_dotenv: ^5.2.1
  hive_flutter: ^1.1.0
  hive: ^2.2.3
  xml: ^6.5.0
  path_provider: ^2.1.5
  hive_ce: ^2.11.3
  hive_ce_flutter: ^2.3.1
  analyzer: ^6.11.0
  app_links: ^6.4.0
  provider: ^6.1.5
  path: ^1.9.1
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  hive_generator: ^2.0.1
  hive_ce_generator: ^1.8.1
  build_runner: ^2.5.4
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/images/
    - assets/icons/
    - .env
  uses-material-design: true

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/logo-police.png"
